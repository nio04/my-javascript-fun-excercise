/*
instruction:

get a input below than 100;
-> 1. if its 'even' -> divide by 2
-> 2. if its 'odd' -> add + 3

the output will be the input for next iteration.
this loop will only stop when its found the main inpu
*/
